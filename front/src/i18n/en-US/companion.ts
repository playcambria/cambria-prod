import type { BaseTranslation } from "../i18n-types";

const companion: BaseTranslation = {
    select: {
        title: "Choose a pet",
        any: "Back",
        continue: "Continue",
    },
};

export default companion;
